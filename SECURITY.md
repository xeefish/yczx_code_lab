# 安全与隐私

## 不得提交

- API Key、访问令牌、密码、验证码和证书私钥。
- `.env`、数据库、真实日志、上传文件和本地私有配置。
- 手机号、学校账号、私人聊天、未经授权的成员资料。
- 包含上述内容的截图、测试数据或 Git 历史。

## 提交前检查

```powershell
git status --short
git diff
git diff --cached
```

只使用明确的文件路径执行 `git add`，初学阶段不要习惯性使用 `git add .`。

## 发现敏感信息

不要继续推送，也不要只通过新增一个删除 commit 处理。立即停止操作并私下联系仓库管理员，说明文件、分支和 commit；不要在公开 Issue 或群聊中再次粘贴敏感值。

## 练习安全

公共示例不访问工作区外路径，不执行破坏性命令。需要模型或网络的示例只能从环境变量读取个人 API Key，并明确成本、权限和停止方式。
