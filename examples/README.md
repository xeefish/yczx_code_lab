# 公共示例目录

本目录存放已经整理、可以供多人重复运行的教学示例。每个项目使用独立子目录，避免依赖和概念混在一起。

## 当前示例

- [`yczx_code/`](./yczx_code/README.md)：真实模型对话、Tool Use、Agent Loop、只读 ReAct Agent 和模块化 Mini Agent。

公共示例应包含明确的学习目标、运行方式、安全边界和来源说明。修改已有示例前先沟通，避免多人同时改动同一文件。
